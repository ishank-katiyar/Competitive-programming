#include <bits/stdc++.h>
using namespace std;

int main() 
{
 ios_base::sync_with_stdio(false);
 cin.tie(nullptr);
 cout.tie(nullptr);
 int d, t, s;
 cin >> d >> t >> s;
 cout << (d <= t * s ? "Yes\n" : "No\n");
 return 0;
}
